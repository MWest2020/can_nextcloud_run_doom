/**
 * SPDX-FileCopyrightText: 2024-present The DoomNextcloud Contributors
 * SPDX-License-Identifier: AGPL-3.0-or-later
 *
 * DoomNextcloud — settings UI (placeholder)
 *
 * This module will handle any user-accessible settings panel within the game view,
 * for example: volume controls, key remapping, resolution selection.
 *
 * PLACEHOLDER: Settings UI is post-MVP. This file is a structural placeholder.
 */

/**
 * Initialize the settings UI (no-op in MVP).
 */
export function initSettings() {
    // PLACEHOLDER: In a future milestone, this will:
    //  1. Read persisted settings from Nextcloud user preferences (OCP\IConfig).
    //  2. Render a settings panel component.
    //  3. Propagate setting changes to the WASM runtime.
    console.info('[DoomNextcloud] Settings UI: placeholder, no-op in MVP.')
}
